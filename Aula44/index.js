//for

for (let i = 0; i < 10; i++) {
    const num = i % 2 === 0 ? 'par' : 'ímpar';
    console.log(`${i} é par? ${num}`);
}

const frutas = ['Banana', 'Maçã', 'Laranja', 'Uva'];

for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
}